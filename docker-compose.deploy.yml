services:
  backend:
    build:
      context: ./backend
    container_name: vibehr-backend
    restart: unless-stopped
    env_file:
      - ./.env.deploy
      - ./.env.deploy.secret
    ports:
      - "127.0.0.1:8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./seed-archive:/app/seed-archive
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health', timeout=3)\""]
      interval: 15s
      timeout: 5s
      retries: 10

  frontend:
    build:
      context: ./frontend
    container_name: vibehr-frontend
    restart: unless-stopped
    env_file:
      - ./.env.deploy
      - ./.env.deploy.secret
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "127.0.0.1:3000:3000"
